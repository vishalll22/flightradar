# Leave blank to use the Vite dev proxy (same-origin /api and /ws).
# In production, point these at your backend, e.g.:
#   VITE_API_URL=https://api.example.com
#   VITE_WS_URL=wss://api.example.com/ws
VITE_API_URL=
VITE_WS_URL=
